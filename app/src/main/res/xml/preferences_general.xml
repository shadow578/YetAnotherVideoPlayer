<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <PreferenceCategory android:title="General" />

    <SwitchPreference
        android:defaultValue="@bool/DEF_BACK_DOUBLE_PRESS_EN"
        android:key="BACK_DOUBLE_PRESS_EN"
        android:summaryOff="Back button closes app without confirmation"
        android:summaryOn="Back button has to be pressed twice to exit"
        android:title="Prevent Accidental Closing" />

    <EditTextPreference
        android:defaultValue="@integer/DEF_BACK_DOUBLE_PRESS_TIMEOUT"
        android:dependency="BACK_DOUBLE_PRESS_EN"
        android:key="BACK_DOUBLE_PRESS_TIMEOUT"
        android:numeric="integer"
        android:summary="How fast you have to press the Back button to exit (milliseconds)"
        android:title="Double Press Timeout" />

    <SwitchPreference
        android:defaultValue="@bool/DEF_PERSIST_VOLUME_EN"
        android:key="PERSIST_VOLUME_EN"
        android:summaryOff="Volume Level does not Persist between Playback Sessions"
        android:summaryOn="Volume Level Persists between Playback Sessions"
        android:title="Persistent Volume" />

    <SwitchPreference
        android:defaultValue="@bool/DEF_PERSIST_BRIGHTNESS_EN"
        android:key="PERSIST_BRIGHTNESS_EN"
        android:summaryOff="Brightness Level does not Persist between Playback Sessions"
        android:summaryOn="Brightness Level Persists between Playback Sessions"
        android:title="Persistent Brightness" />
</PreferenceScreen>